# 补充实验并行任务入口

更新：2026-09-15，已同步 C3、B5 与 F 回执。

当前唯一执行索引为 [Safe-pair恢复后的当前任务入口](paper1_server_codex_post_audit_task_index.md)。

最终恢复报告已达到300条executable、`k=40`。C3 已完成最终 E1 digest 绑定（`d148...732a`），B5 已确认 `E1_CONSUMER_PASS` / `READY_FOR_RUN`，F 已完成最终 Core run 的 `prepare + build-directions` 并记录 `CORE_DIRECTIONS_READY`。旧 source 恢复、全量 safe-pair 审核和 C3/B5/F 均不再执行。

旧A/B/E/C2/B4/C3/B5/F任务书仅作记录。B4 smoke 仍是 `NON_EVIDENCE`；本次同步不启动 screen 或 evaluation。下一步只能通过单独授权的 Core development screen 任务选择 A/S 剂量，正式 generation、Judge、analysis、human audit 和 archive 仍未开始。
