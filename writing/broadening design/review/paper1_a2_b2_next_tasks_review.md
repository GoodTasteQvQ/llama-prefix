# A2/B2 报告复核与后续任务边界审阅

日期：2026-09-14。审阅对象：当前任务索引、A3/B3/E v2任务书、Notion状态表。本文件是本机文档/报告审阅，不是服务器代码验收或正式实验放行。

## 证据与决策

已完整读取本机 `paper1_safe_pair_re_review_report.md`，并与先前 A/B/C/D 报告、B2/A2任务书、科学设计和本机实现对照。`git fetch origin` 后，本机/远端仍为 `1124c53`。本机和远端树均没有 `paper1_implementation_contract_repair_report.md`、`mbd_nm_v211_public.json`、`rebuild_public_safe_pair_ledger_v2.py`；本机仍是修复前的config/snapshot/semantic gate。不能依据“43 passed”的会话输出声称审阅了B2源码或测试。

A2报告数字内部一致：416−7=409，221+188=409，409×2=818，`floor((221−100)/5)=24`；门槛250，差29条executable。A/B边际include数293/285和交集221也不矛盾；详细交叉表和请求真实性需实际记录验证。报告的818请求/独立身份/provenance目前属于服务器声明，原始材料未同步，本机未重新执行核验。

因此暂不启动正式实验，也不再次全量重审。当前最快的有依据路径是三线：A3验证已有审核并交付；B3补交实际实现并接入已存在资产；E先调查少量新增来源，A3/B3通过后可以继续到数据gate，不必再次申请启动恢复。

旧v1的194条仍属失效历史。原恢复v1任务中的“保留194/缺56/v1提示”已移除，恢复v2只认A3核验的A2基线。当前尚未选定新的公开来源，任务书没有把未查证数据集写成已推荐数据集。

## 有边界自审

| 检查 | 发现与处理 | 结论 |
|---|---|---|
| 证据层级 | 明确B2报告/代码缺失，A2是报告层复核；以交付物核验替代无根据的验收声明 | 通过 |
| 数量与旧历史 | 使用221/k24/差29；失效194不复用；preliminary409与executable221分别计数 | 通过 |
| 是否过度复审 | A3仅全量结构检查+至多6对诊断，不发新reviewer；不因数量少翻转旧判定 | 通过 |
| 是否过度工程 | 不建通用审计系统，不重复下载/全模型hash/Gemma探针；已有测试与源码对应时可复用 | 通过 |
| 并行冲突 | A3只读原件；B3先独占实现写入；E先独立来源调查，B3停止后再接手实现/source | 通过 |
| 新来源选择 | 最多2 bundles，每批100，共至多200新pairs/400 canonical审核请求；按上游固定顺序，不按结果选源/选行 | 通过 |
| 可选停止偏差 | 每批集合在review前保存，整批双审结束才判断够数；达到门槛停止启用新批，不在一批内部挑够250就停 | 通过 |
| 旧记录复用 | 前416行/逐条来源不变，aggregate hash与input identity分开；不得伪造旧请求hash/time/id；自审补充了第二批须与第一批全部行去重，避免跨来源重复计数 | 通过 |
| 审核来源与元数据 | 真实隔离subagent；HTTP429与重试计数分开；隐藏指令/未暴露参数不提取、不伪造、不强求 | 通过 |
| 数据与科学gate | development100/5folds/k>=30及20,480预算不变；机器审核不冒充人工；不能靠空分割disjoint放行 | 通过 |
| E1/E2边界 | 资产接线和runtime探针不能当正式gate；E1最终frame等待实际safe分割，不回头按E1改safe source | 通过 |
| 可交付性 | B3必须交真实diff和untracked文件；A3/E交ledger/索引/prepare及raw包，不能再次只同步摘要 | 通过 |

本轮仅修改文档，未运行Python测试、prepare或模型，也未改实验代码/科学设计正文。完成文档路径/关键条件/差异检查；服务器新任务运行结果不由本审阅预先保证。

审阅结论：`DOCUMENT_REVIEW_PASSED`。允许交付当前三份任务书；`FORMAL_EXPERIMENTS_NOT_RUN`。
