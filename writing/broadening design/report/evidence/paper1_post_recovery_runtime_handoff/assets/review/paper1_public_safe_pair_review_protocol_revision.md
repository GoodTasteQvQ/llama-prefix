# Public safe-pair review protocol revision

旧 v1 记录因虚假或缺失 provenance 失效，全部标记为 `INVALID_REVIEW_HISTORY`。

新 `public-semantic-pair-quality-v2` 仅修复真实 Codex subagent 执行和请求级记录，不改变
harmful unsafe、harmless benign、pair comparable 和 evaluation overlap 四项科学判定标准，
也不改变 unanimous include 的裁决规则。
