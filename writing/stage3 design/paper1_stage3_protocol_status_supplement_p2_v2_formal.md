# Paper 1 Stage 3 P2 v2 Formal Status Supplement

Supplement date: `2026-08-23`
Status: `FORMAL AMENDED T/H P2 RESULT`

## Registration

The completed run
`p2-qwen25-amended-th-paper-20260822T051005Z-v2` is registered as the formal
amended T/H P2 result. Before that run began, the v2 protocol fixed the
endpoints as:

1. `P2-U(T) = unsafe(all,T) - unsafe(content,T)`
2. `P2-B(H) = broken(all,H) - broken(content,H)`

The prompt, vector, model, dose, generation and judge configurations, retry
policy, 4,000 identities, and paired matching remain exactly those of the
executed v2 design.

## Limited supersession

This supplement supersedes only the v2 restrictions that conflict with the
result status and post-processing authority stated here. The amended run is no
longer restricted to descriptive-only follow-up status. Formal result
materialization and statistics within the original v2 design may be performed
on the existing raw result. This authority creates no new generation, judge,
or model call and does not retroactively alter the execution.

Nothing here changes the anchors, sample size, prompt/vector frame, dose,
retry policy, pairing rules, or endpoints. Any later statistics or K1 work may
read only the existing v2 raw result and must not generate or regenerate model
output.

## Historical separation and immutability

The original P2 remains the historical old result. Because its endpoint
mapping does not answer the amended research question, it must be preserved
and reported separately; it must not be deleted, overwritten, or rerun.

The v2 run directory, run manifest and identity metadata, raw records, and
existing materialized raw result are immutable. Their original runtime fields,
including `paper_result_eligible=false`, remain exactly as recorded. This
supplement supplies the subsequent interpretation and formal result
registration basis; it does not modify, rewrite, or fabricate runtime fields.

## Reporting rule

The result must be reported under the same rule whether it supports, opposes,
or does not clearly support the motivating expectation. No causal,
general-safety, or significance claim or rule, and no result-directed selection
rule, may be added.
