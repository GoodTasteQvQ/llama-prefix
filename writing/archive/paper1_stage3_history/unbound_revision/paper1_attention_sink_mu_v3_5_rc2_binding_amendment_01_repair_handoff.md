# v3.5-rc2 binding amendment 01 repair handoff

Status: `DESIGN-FREEZE-FAIL / REPAIR-ONLY`

Date: 2026-07-22

This document is a repair handoff for the two original closure roots only:

- `P0-STAT-01`: matched two-phase simultaneous CI binding;
- `P0-STAT-02`: retained K1 / harmful-clean / benign bootstrap binding.

It does not authorize a new protocol version, a new experiment, or a wider
design review. The base specification remains the combined specification:

```text
v3.5-rc2 + v3.5-rc2-binding-amendment-01
```

## 1. Frozen repair boundary

The repair must preserve all of the following:

- `P=50`, `V=20`;
- human response items `N=720`;
- logical generation `5,580`;
- first-pass scheduled judge `5,580`;
- no new model, prompt, vector, anchor, endpoint, or experiment block;
- no model-based RD/CI, power/coverage simulation, selector, K2, V2,
  attention block, rendering 2x2, stochastic decoding, or phase confirmation;
- no p-value, rejection decision, significance claim, confirmation claim, or
  change to the P1/P2/K1/benign/human estimands;
- no formal experiment data in fixtures;
- fixed expected goldens must not be regenerated by the implementation under
  test.

The prior audit independently confirmed that the rc2 base hashes, scope, and
budget remain intact. Do not change any base rc2 artifact to fix the failures
below.

## 2. Executive failure summary

The current amendment package has one artifact-identity root affecting both
normative references, plus one executable mismatch inside each reference.

| Root | Current state | Closure impact |
|---|---|---|
| Two-phase reference identity | Manifest expects `ce9ecb0cff085279b80fdd847634b1b3f1cb4a553c731f9dcd2b0150c7e72ecf`, 51,899 bytes; current file is `9eee944f3e17e3412e2efd3bafe5573072c37390042e58e6fa1be636b9d7f31c`, 58,358 bytes | `P0-STAT-01` not closed |
| Retained-bootstrap reference identity | Manifest expects `a39bb5d6f1e9bc223f7be63410a29f199d814e0da71c913f2467dfd102aa78b1`, 42,426 bytes; current file is `be72e2bfb054c9119f487197b9d04cf84973354e54dd3e82c8617ffe4cfb2794`, 46,979 bytes | `P0-STAT-02` not closed |
| Two-phase executable behavior | Bound valid success input fails before CI execution with `SCHEMA_INVALID:$.members:items` | Complete CI and failure-precedence goldens do not execute |
| Retained executable behavior | Current success and failure outputs do not equal the fixed expected goldens; the invalid-denominator case emits the wrong first code | Retained exact-output and failure-mapping contract not closed |

All three existing verifiers currently return exit code 1. This is an expected
and useful proof that manifest mismatch is fail-closed.

## 3. Repair P0-STAT-01

### 3.1 Binding mismatch

Normative source:

```text
scripts/stage3_design/v3_5_rc2_binding_amendment_01/two_phase/two_phase_reference.py
```

Stale bindings:

- `writing/stage3 design/v3_5_rc2_binding_amendment_01/two_phase/two_phase_binding_manifest.json`
  around lines 34 and 41-44;
- `writing/stage3 design/v3_5_rc2_binding_amendment_01/amendment_binding_manifest.json`
  line 75;
- `writing/stage3 design/v3_5_rc2_binding_amendment_01/amendment_release_manifest.json`
  line 70.

The dedicated verifier fails before golden execution with:

```text
length mismatch: scripts/stage3_design/v3_5_rc2_binding_amendment_01/
two_phase/two_phase_reference.py
```

### 3.2 Schema-validator defect

The bound schema uses Draft 2020-12 tuple validation:

```text
writing/stage3 design/v3_5_rc2_binding_amendment_01/two_phase/
two_phase_input.schema.json:29-38
```

Specifically, it defines two `prefixItems` and then sets `items: false`.
The current validator at:

```text
scripts/stage3_design/v3_5_rc2_binding_amendment_01/two_phase/
two_phase_reference.py:178-192
```

treats every non-object `items` value as an invalid schema. It does not process
`prefixItems`, so the reference rejects its own valid success fixture with:

```text
INPUT_INVALID_NON_ESTIMABLE:SCHEMA_INVALID:$.members:items
```

### 3.3 Required repair

Preferred minimal path:

1. Restore the exact previously bound reference bytes with SHA256 `ce9ecb0...`,
   if that reviewed source is available.
2. Run the dedicated verifier before changing any manifest.

If the current source is intentional and must be retained:

1. Implement the schema constructs actually used by the bound schema.
2. Validate each present array position against the corresponding
   `prefixItems` schema.
3. Interpret `items: false` as prohibiting positions beyond `prefixItems`, not
   as `SCHEMA_INVALID`.
4. Preserve the existing semantic validation, first-failure order, RNG,
   estimator, and CI code.
5. Confirm that the unchanged fixed success and failure expected files compare
   exactly before refreshing hashes.

Do not merely record the current broken file hash in the manifests.

### 3.4 P0-STAT-01 acceptance after repair

The existing verifier must reach the full implementation call at
`verify_two_phase_golden.py:213-218` and print:

```text
TWO_PHASE_AMENDMENT_GOLDEN_PASS
```

Required observed values include:

- prompt two-phase common successes: `9,999`;
- vector-aware common successes: `9,988` for the fixed synthetic fixture;
- `9,499` boundary: failure;
- `9,500` boundary: pass;
- prompt and vector full CI outputs equal the fixed expected file;
- all 15 fixed failure cases return their exact expected codes;
- census, vector, RNG-byte, multiplicity, shared-stream, and clipping witnesses
  remain unchanged.

The fixed expected point estimates can be checked manually:

```text
P2-U(A) = (5/15 + 1.5/15) - (3/15 + 1/15) = 1/6
P2-B(T) = (4/15 + 1.5/15) - (3/15 + 4.5/15) = -2/15
```

## 4. Repair P0-STAT-02

### 4.1 Binding mismatch

Normative source:

```text
scripts/stage3_design/v3_5_rc2_binding_amendment_01/retained_bootstrap/
retained_bootstrap.py
```

Stale bindings:

- `writing/stage3 design/v3_5_rc2_binding_amendment_01/retained_bootstrap/
  retained_bootstrap_manifest.json` around line 51;
- `writing/stage3 design/v3_5_rc2_binding_amendment_01/
  amendment_binding_manifest.json` line 73;
- `writing/stage3 design/v3_5_rc2_binding_amendment_01/
  amendment_release_manifest.json` line 68.

The dedicated verifier currently exits nonzero with:

```text
HASH_MISMATCH scripts/stage3_design/v3_5_rc2_binding_amendment_01/
retained_bootstrap/retained_bootstrap.py: be72e2bfb054c9119f487197b9d04cf84973354e54dd3e82c8617ffe4cfb2794
```

### 4.2 Fixed success-output mismatch

The fixed success expected file requires `canonical_output_sha256` for K1,
harmful-clean, benign-broken, and zero-SD outputs at approximately lines 39,
77, 89, and 99:

```text
writing/stage3 design/v3_5_rc2_binding_amendment_01/retained_bootstrap/
fixtures/success_golden_expected.json
```

The current `evaluate_success_fixture` at
`retained_bootstrap.py:824-878` returns a different projection containing
additional frame/audit fields and omits those four expected digest fields.
Consequently the canonical success comparison is false.

### 4.3 Invalid-denominator precedence mismatch

The fixed failure expected file requires:

```json
{"case_id":"invalid_profile_denominator","code":"INVALID_DENOMINATOR","detail":"profile denominator is zero"}
```

Evidence:

```text
writing/stage3 design/v3_5_rc2_binding_amendment_01/retained_bootstrap/
fixtures/failure_golden_expected.json:8
```

The fixture mutation empties the harmful-clean records. The current analyzer
checks complete prompt coverage at `retained_bootstrap.py:576-580` before
calling `_profile_point`, and therefore emits:

```text
INPUT_SCHEMA_INVALID: harmful-clean must contain each frozen prompt exactly once
```

It never reaches the bound `INVALID_DENOMINATOR` mapping. The canonical failure
comparison is therefore false.

### 4.4 Required repair

Preferred minimal path:

1. Restore the exact previously bound source bytes with SHA256 `a39bb5d6...`,
   if that reviewed source is available.
2. Run the dedicated verifier against the unchanged fixed expected files.

If the current source is intentional and must be retained:

1. Restore the exact success-fixture projection required by the fixed expected
   file, including the four `canonical_output_sha256` fields.
2. Make the registered `invalid_profile_denominator` mutation return the fixed
   `INVALID_DENOMINATOR` code before any competing coverage error, or replace
   the mutation with an independently reviewed case that reaches the same
   normative denominator branch.
3. Keep K1 product weights and the shared prompt/vector draw unchanged.
4. Keep harmful-clean equal-size prompt resampling unchanged.
5. Keep benign vector averaging before prompt bootstrap unchanged.
6. Keep 10,000 replicates, 9,500 common success, nearest-rank percentile CI,
   degenerate zero-SD intervals, and the no-testing claim boundary unchanged.
7. Compare against independently maintained fixed expected files. Do not call
   the implementation to rewrite them.

### 4.5 P0-STAT-02 acceptance after repair

The dedicated verifier must print:

```text
RETAINED_BOOTSTRAP_GOLDEN_PASS
```

It must establish all of the following in one run:

- K1, harmful-clean, benign-broken, and zero-SD success fixtures execute all
  10,000 replicates;
- success output equals the existing fixed expected file canonically;
- failure output equals the existing fixed expected file canonically;
- `invalid_profile_denominator` returns exactly `INVALID_DENOMINATOR`;
- `9,499` fails and `9,500` passes;
- exact RNG seed/word/draw witnesses and multiplicity witnesses match;
- zero bootstrap SD produces only a flagged degenerate interval.

Manual benign witness:

```text
d_1 = 5/20  - 0 =  0.25
d_2 = 10/20 - 1 = -0.50
d_3 = 15/20 - 0 =  0.75
mean(d_p) = 1/6
```

## 5. Manifest refresh order

Only refresh identities after both component verifiers pass against fixed
goldens. Update atomically in this order:

1. Component implementation hash/length and producer fields:
   - `two_phase/two_phase_binding_manifest.json`;
   - `retained_bootstrap/retained_bootstrap_manifest.json`.
2. Recompute each component manifest `self_sha256` using its documented
   canonical-JSON rule.
3. Update the component-manifest and implementation entries in
   `amendment_binding_manifest.json`.
4. Recompute `amendment_binding_manifest.json` `self_sha256`.
5. Update `amendment_release_manifest.json`, including its binding-manifest
   hash/length and all changed component artifacts.
6. Recompute the release manifest `self_sha256`.
7. Update `audit_disposition.md` only after every verifier returns zero.

Do not alter base rc2 hashes or use an execution-specific A0/I0 manifest to
justify a design change.

## 6. Required verification commands

Run from `D:\llama_prefix`. Redirect temporary files before every verifier:

```powershell
$codexTemp = Join-Path (Get-Location) ".codex-temp"
New-Item -ItemType Directory -Force $codexTemp | Out-Null
$env:TEMP = $codexTemp
$env:TMP = $codexTemp
$env:NX_DAEMON = "false"
$env:PYTHONDONTWRITEBYTECODE = "1"

python -B "scripts/stage3_design/v3_5_rc2_binding_amendment_01/two_phase/verify_two_phase_golden.py"
python -B "scripts/stage3_design/v3_5_rc2_binding_amendment_01/retained_bootstrap/verify_golden.py"
python -B "scripts/stage3_design/v3_5_rc2_binding_amendment_01/verify_amendment.py"
```

Required final state:

```text
TWO_PHASE_AMENDMENT_GOLDEN_PASS
RETAINED_BOOTSTRAP_GOLDEN_PASS
AMENDMENT_STATIC_VERIFICATION_PASS
```

The aggregate report must also retain:

- `base_files_unchanged = 25`;
- `formal_data_used = false`;
- `no_scope_expansion = true`;
- logical generation `5,580`;
- first-pass scheduled judge `5,580`;
- human items `720`;
- all prohibited restoration flags `false`.

## 7. Completion checklist

- [ ] Two-phase valid input reaches both full simultaneous-CI implementations.
- [ ] Two-phase success, failure, census, vector, RNG, 9,499, and 9,500 goldens pass.
- [ ] Retained K1, harmful-clean, benign, RNG, multiplicity, failure, and
      degenerate goldens pass.
- [ ] The invalid-denominator failure returns the exact registered code.
- [ ] Fixed expected files remain independent from the tested implementation.
- [ ] Component manifests bind every code/schema/fixture dependency.
- [ ] Aggregate binding and release manifests contain current hashes and valid
      self hashes.
- [ ] Manifest mismatch still causes nonzero verifier exit.
- [ ] Base hashes, scope scan, `P=50`, `V=20`, `720`, and `5,580/5,580` remain
      unchanged.
- [ ] No formal experiment was executed and no formal data entered fixtures.

Closure is achieved only when all three verifier commands return exit code 0.
